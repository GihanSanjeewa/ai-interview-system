__pycache__/
*.py[cod]
venv/
.env

# Local audio + CV scratch files
temp.mp3
temp_cv.*

# Training artefacts — keep out of git, they're huge
data/
models/
eval-report.csv
*.safetensors

# Raw downloaded datasets — hundreds of MB; re-fetch with dataset/download_datasets.py
dataset/raw/

# QLoRA training outputs — adapters, checkpoints, metrics (regenerable)
training/output/

# Reproducibility snapshot regenerated by notebooks/01_qlora_data_preparation.ipynb
# (manifest.json is kept — it carries the statistics and library versions)
dataset/training/qlora_sft/*.jsonl
