"""Sinhala Whisper fine-tuning utilities.

These scripts are intended to run **off-process** on a GPU (Colab, kaggle, or
a workstation with CUDA). They are imported only by the CLI entry points, so
the inference path never pulls the training dependencies.
"""
